'use strict';

// Configuring the Articles module
angular.module('articles').run(['Menus',
	function(Menus) {
		// Set top bar menu items
		
		/*Menus.addMenuItem('topbar', 'Selfie Map', 'articles', 'dropdown', '/articles(/create)?');
		Menus.addSubMenuItem('topbar', 'articles', 'Your Selfie Locations', 'articles');
		Menus.addSubMenuItem('topbar', 'articles', 'New Selfie Location', 'articles/create');
	*/}
]);